152vpn.ru, www.152vpn.ru {
	encode gzip zstd

	header {
		# Security headers
		X-Content-Type-Options nosniff
		Referrer-Policy strict-origin-when-cross-origin
		X-Frame-Options SAMEORIGIN
		-Server
	}

	reverse_proxy app:8787
}
